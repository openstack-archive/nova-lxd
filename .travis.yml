language: python
python:
  - "3.4"
  - "2.7"
install: pip install -U -r requirements.txt -r test-requirements.txt
before_script: flake8 --ignore=E123,E125,H904
script: ostestr
cache:
    directories:
        - $HOME/.cache/pip
