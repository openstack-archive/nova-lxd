============
Installation
============

At the command line::

    $ pip install nova-compute-lxd

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv nova-compute-lxd
    $ pip install nova-compute-lxd